#include "resource.h"

// The icon font is embedded rather than loaded from disk: Glacier is a single
// injected DLL with no install directory to put a companion file in.
IDR_FONT_ICONS RCDATA "..\\assets\\fonts\\fa-solid-900.ttf"
